#version 440

out vec4 color;
in vec3 v_normal;

void main() {
    vec4 I = vec4(0.8, 0.5, 0.5, 1.0);
    vec3 light_dir = normalize(vec3(0.3, 0.3, 0.3));
   
    // TODO : Complete light equation
    color = I * dot(v_normal, light_dir) * 0.3 + vec4(0.2, 0.8, 0.8, 1);
}